# Google Maps is no longer required for location picking.
# Magellan uses OpenStreetMap tiles without an API key.
# Keep this only if a future Google Maps integration is intentionally enabled.
VITE_GOOGLE_MAPS_API_KEY=
